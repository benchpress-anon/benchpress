# SAM 3 + Hugging Face Inpainting Video Editor
# Core dependencies

# Video I/O
opencv-python>=4.8.0
imageio>=2.31.0
imageio-ffmpeg>=0.4.9

# Deep Learning - IMPORTANT: versions must match!
# torch 2.9.1 + torchvision 0.24.1 — tested working with DiffuEraser on MPS
torch==2.9.1
torchvision==0.24.1
torchaudio==2.9.1

# Hugging Face
# SAM3 support is in transformers >=5.3.0 on PyPI; pinned for reproducibility.
transformers==5.3.0
diffusers>=0.32.0
accelerate>=0.25.0
safetensors>=0.4.0
huggingface_hub>=1.0.0

# Image processing
Pillow>=10.0.0
numpy>=1.24.0
scipy>=1.11.0

# Utilities
tqdm>=4.66.0
einops>=0.8.0
decord>=0.6.0
